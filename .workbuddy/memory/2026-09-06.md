# 2026-09-06

- 更新 CMakeLists.txt：
  - 修复跨平台编译编码问题：MSVC 使用 `/utf-8`，GCC/Clang 使用 `-finput-charset=UTF-8 -fexec-charset=UTF-8`
  - 项目名从 `std_string` 统一改为 `string_algo`（CMake project 名、导出目标、命名空间、包路径全部更新）
  - 修复 CMake 错误：`add_library(string_algo INTERFACE)` 移到所有 `target_*` 命令之前
- 创建 .gitignore，清理 build 产物和二进制文件
- 推送代码到 GitHub：https://github.com/qq1134993111/string_algo
- 创建 README.md，包含特性、用法、构建说明、项目结构
